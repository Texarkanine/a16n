---
task_id: issue-158
complexity_level: 1
date: 2026-08-20
status: completed
---

# TASK ARCHIVE: Clear leftover oxlint unused-vars and irregular-whitespace in plugin-cursor

## SUMMARY

Cleared the 10 Oxlint leftovers in `@a16njs/plugin-cursor` tracked as [#158](https://github.com/Texarkanine/a16n/issues/158): 6 `no-unused-vars` and 4 `no-irregular-whitespace`. `pnpm exec oxlint packages/plugin-cursor` is clean; `pnpm --filter @a16njs/plugin-cursor test` is 21 files / 191 passed.

## REQUIREMENTS

From the project brief and [#158](https://github.com/Texarkanine/a16n/issues/158):

- Make `pnpm exec oxlint packages/plugin-cursor` clean (unused-vars and irregular-whitespace only).
- Keep `pnpm --filter @a16njs/plugin-cursor test` passing.
- Preserve meaning when fixing irregular whitespace.
- Do not write change-detector tests; oxlint and existing tests are the validation.
- Do not enable extra Oxlint categories. Do not add lint to CI.

## IMPLEMENTATION

Follow-up to #74 / #155. `oxlint --fix` does not clear these leftovers.

The four irregular-whitespace hits were U+200B zero-width spaces in block comments, inserted so the glob `.cursor/skills/*/SKILL.md` would not terminate `/* */`. Those comments now say `.cursor/skills/<name>/SKILL.md`, matching the skill-directory model in `systemPatterns.md`. Line comments and markdown can keep the `*/` glob.

Unused bindings:

- Dropped unused `type FileRule` import in `src/emit.ts` (`isFileRule` remains).
- Dropped unused `path` import in `test/emit-simple-agent-skill.test.ts`.
- Dropped unused `result` bindings in four tests that already assert on written files (same pattern as neighboring cases).

Files:

- `packages/plugin-cursor/src/skill-field-support.ts`
- `packages/plugin-cursor/src/discover.ts`
- `packages/plugin-cursor/src/emit.ts`
- `packages/plugin-cursor/test/emit-skills.test.ts`
- `packages/plugin-cursor/test/emit-agent-ignore.test.ts`
- `packages/plugin-cursor/test/emit-agent-skill-io.test.ts`
- `packages/plugin-cursor/test/emit-manual-prompt.test.ts`
- `packages/plugin-cursor/test/emit-simple-agent-skill.test.ts`

No creative-phase documents. This was a Level 1 lint cleanup.

## TESTING

No new Vitest cases (lint is the tester; no change-detectors).

- `pnpm exec oxlint packages/plugin-cursor` — clean (exit 0).
- `pnpm --filter @a16njs/plugin-cursor test` — 21 files / 191 passed after `pnpm --filter @a16njs/models build` (worktree had `node_modules` but no `models/dist`).
- `/niko-qa` — PASS (KISS/DRY/YAGNI/completeness/regression/integrity/docs). No QA fixes.

## LESSONS LEARNED

Inlined from `reflection/reflection-issue-158.md`:

- Oxlint `no-irregular-whitespace` flags U+200B. In this package that character existed only so `.cursor/skills/*/SKILL.md` would not close a block comment. `.cursor/skills/<name>/SKILL.md` keeps the meaning.
- Leftover oxlint tickets do not need new tests; oxlint is the failing check.
- Worktrees still need `pnpm --filter @a16njs/models build` before plugin tests resolve `workspace:*`.
- Million-dollar question: nothing notable. A lint-clean comment that never writes `*/` inside a block comment is the form this should have had from the start.

## PROCESS IMPROVEMENTS

Level 1 normally skips reflect and archive. This leftover-wave child ran both under standing consent so the parent L4 run has a written record. That is a wave-process choice, not a change to the Level 1 workflow.

## TECHNICAL IMPROVEMENTS

None. Extra Oxlint categories and CI lint remain out of scope until the leftover tickets (#156–#161) are gone.

## NEXT STEPS

None for this package. Remaining leftover oxlint tickets are other packages. Operator opens the PR to `main`.
