# Progress

## Summary
Fix three related issues:
1. Docs in `packages/docs/docs/plugin-{cursor,claude}/index.md` inaccurately describe complex-skill handling.
2. `--rewrite-path-refs` does not rewrite references to ride-along resource files inside an `AgentSkillIO`'s SKILL.md body.
3. `--rewrite-path-refs` does not rewrite references inside the ride-along files themselves. Fix scoped to the two AgentSkills.io-spec text subtrees (`scripts/` and `references/`); `assets/` and any other subtree remain untouched by design.

**Complexity:** Level 2

## History
- 2026-04-20: Task classified as Level 2 after intent clarification.
- 2026-04-20: Preflight PASS w/ ADVISORY. Plan amended (Behavior 1, step 6, step 7, step 8) to capture full classification priority for both Cursor and Claude skip conditions and to include docs-site build in validation. Three advisory findings recorded.
- 2026-04-20: Plan further amended to include `buildMapping` collision-detection lint (new Behavior 2b, updated step 2, new challenge). Return type of `buildMapping` changes to `{ mapping, warnings }`. Single caller in `packages/engine/src/transformation.ts:112` updates to destructure. Captured the future clean-break refactor as a punchlist in `planning/writtenfile-clean-break.md` for a later Level 3 task.
- 2026-04-20: Post-preflight scope expansion (user request, driven by AgentSkills.io spec §Optional directories). Flipped "rewrite refs inside ride-along files" from non-goal to bounded in-scope. Added Behaviors 7 + 8, Step 2b (engine: `rewriteContent` + `detectOrphans` extended with local `isRewritableSkillResource` helper gating on `scripts/**` | `references/**`), expanded Step 5 integration test with assets/unknown negative cases, added Step 8 (docs: "What gets rewritten" table in `packages/docs/docs/cli/index.md`, mirror in understanding-conversions). Complexity remains Level 2. `projectbrief.md` non-goals updated.
- 2026-04-20: BUILD complete. All 9 planned steps implemented and committed (commits 6c7337c5..327c4ee2). Full validation: build 7/7 pass, typecheck 12/12 pass, **tests all 15 turbo tasks pass on Node 22** (the project's only supported runtime per engines.node>=22 and .nvmrc=22). Docusaurus site build deferred to user per request. (Initial local test run accidentally used Node 20, which makes `engine > plugin-discovery > 'fall back to index.js when no package.json exists'` fail because Node 20 defaults `--experimental-detect-module` to off. Under Node 22 everything is green. No regressions introduced.)
- 2026-04-20: QA phase — trivial fixes applied. Verified on Node 22 that CI1–CI4 integration tests all pass (contradicting the mid-build "Workspace refactor blocker" narrative, which was a Node 20 artifact, not a real failure). Removed the stale NOTE scaffolding comment inside the CI4 test describing that false blocker, and corrected activeContext.md to reflect actual status. Only unrelated failure in full suite is `CLI > --help > should show help when invoked through a symlink`, a pre-existing CLI symlink issue out of scope for this task.
- 2026-04-21: During QA follow-up, investigated the `--help through a symlink` flake. Root cause: `cli/src/index.ts` used `realpathSync`-string-compare for main-module detection; under transient FS errors the catch-fallback returned the unresolved symlink path and the CLI silently exited 0 with empty stdout — affecting every `npx a16n` / `node_modules/.bin/a16n` invocation, not just the test. Replaced with device+inode `statSync` comparison (`isSameFile`). Full CLI suite now green across 5 consecutive runs. Pushed-back-and-justified: the fix is in `index.ts` proper because symlinked invocation IS the production shape. Long-term cleanup (split library `index.ts` from executable `run.ts` so the heuristic disappears entirely) captured in `planning/cli-entry-point-split.md`.
- 2026-04-21: New Level 2 task opened — `20260421-preserve-filename-case` — as a PR #84 rework. Scope: preserve source filename case for rule emits in both plugins (AgentSkills.io spec only mandates lowercase for skill **directory** names, not rule filenames); add case-insensitive collision detection to preserve safety on case-insensitive filesystems. Explicitly out of scope: extending rewrite allowlist to `resources/` (Niko-repo bug, separate), Cursor skill dir case (deferred). Prior task (`20260420-skills-docs-and-rewrite-resources`) archive deferred — both tasks will ship in the same PR and can be archived back-to-back when this one completes. Plan written to `tasks.md`; next phase is Preflight.
- 2026-04-21: REFLECT complete (prior task). Reflection written to `memory-bank/active/reflection/reflection-20260420-skills-docs-and-rewrite-resources.md`. Reconciled persistent files: surgical update to `memory-bank/systemPatterns.md` describing `WrittenFile.sourcePaths` precedence over `sourceItems[*].sourcePath` for path-reference rewriting and the collision-detection lint (`Approximated`) added by this task. No `productContext.md` or `techContext.md` changes needed. Key reflection insights: (1) Node-version drift caused narrative pollution, not just test flakes — validation should assert Node version before committing phase narrative; (2) running the full test suite in QA (not just changed-packages subset) caught both the false-blocker narrative and the latent CLI symlink bug; (3) the `WrittenFile.sourcePaths` optional-additive design with collision-lint safety net worked as intended; the "foundational" version (Flavor B from `planning/writtenfile-clean-break.md`) is already captured as a pre-1.0 follow-up. Ready for `/niko-archive`.
- 2026-04-21: PREFLIGHT complete for `20260421-preserve-filename-case`. Status: **PASS with ADVISORY** (initial). Verified call-site line numbers against live source; confirmed no fixture has a mixed-case stem (so B10 is satisfied without fixture churn); confirmed no docs reference rule-filename casing (step 5 is likely a no-op).
- 2026-04-21: QA PASS for `20260421-preserve-filename-case`. Semantic review against the plan found no substantive issues; no trivial fixes needed (build output was clean). All 11 plan behaviors (B1–B11) tested and passing. Full validation on Node 22: 15/15 turbo tasks green (CLI 175/175, engine 173/173, plugin-claude 144/144, plugin-cursor 137/137, plus models/glob-hook/plugin-a16n). Typecheck 12/12 pass. Operator confirmed real-world repro: `/tmp/niko-test` tree shows `activeContext.md`, `productContext.md`, `systemPatterns.md`, `techContext.md`, `memory-bank-paths.md` etc. preserved in `.claude/rules/**`, skill dirs lowercase per spec. QA-phase env note: Cursor agent shell's PATH has `/home/mobaxterm/.cursor-server/bin/<hash>/node` (v20.18.2) earlier than nvm's v22.15.0, which shadowed nvm's node inside the agent specifically. Fixed for QA by prepending the nvm path. Ready for `/niko-reflect`.
- 2026-04-21: REFLECT complete for `20260421-preserve-filename-case`. Reflection written to `memory-bank/active/reflection/reflection-20260421-preserve-filename-case.md`. Reconcile-persistent check: no updates required. `systemPatterns.md` is already current (prior task's reconcile added `WrittenFile.sourcePaths` + collision-lint coverage, and this task's intent-split for filename sanitizers is plugin-internal, not system-level). `productContext.md` and `techContext.md` unaffected. Key reflection insights: (1) Naming-driven intent encoding (`sanitizeRuleFilename` / `sanitizeRuleStem` / `sanitizeSkillDirName`) makes wrong-helper use at a new call site physically awkward — cheap win for a cross-cutting concern. (2) Case policy is per-plugin business, not a shared-models concern — plugin-local collision detection preserves `@a16njs/models@0.12.0`'s semver contract and lets future plugins pick their own stance. (3) Operator plan-review between preflight and build is high-leverage — the semver-bug catch and B8 recharacterization (Cursor skill dirs are spec-mandated, not a16n convention) came from re-reading the plan after preflight PASS, and would have been expensive to find later. (4) The Cursor-agent Node-shadowing (`cursor-server/bin/<hash>/node` v20 ahead of nvm v22 on PATH inside the agent shell) is now a recurring pattern, second task in a row — worth capturing in techContext.md or a CI env-check if it costs us again. Both this task and `20260420-skills-docs-and-rewrite-resources` now reflected; both can be archived back-to-back since they ship in the same PR #84. Ready for `/niko-archive`.
- 2026-04-21: BUILD complete for `20260421-preserve-filename-case`. All 6 plan steps implemented: plugin-claude `sanitizeFilename`/`sanitizeName` split into `sanitizeRuleFilename`/`sanitizeRuleStem`/`sanitizeSkillDirName` (case-preserving for rule filenames, lowercasing retained for skill dirs per AgentSkills.io spec); plugin-cursor `sanitizeFilename` body widened to `[^A-Za-z0-9]+` and inline GlobalPrompt stem case-preserved; case-insensitive collision detection added plugin-locally in both plugins (plugin-claude: new `getUniqueFilenameCI`, dropped `getUniqueFilename` import from `@a16njs/models`; plugin-cursor: private `getUniqueFilename` body change). 19 new tests covering B1–B11 added to both plugins' `emit.test.ts`; 2 pre-existing cursor tests updated to assert the corrected (case-preserving) behavior. Full validation on Node 22: 15/15 turbo tasks pass (144 plugin-claude, 137 plugin-cursor, 173 engine, 175 CLI integration, etc.). Manual repro confirmed: `a16n convert --from cursor --to claude --from-dir src --to-dir out --rewrite-path-refs` against a temp tree mirroring this repo's `.cursor/rules/shared/niko/memory-bank/active/activeContext.mdc` produces `out/.claude/rules/shared/niko/memory-bank/active/activeContext.md` (CamelCase preserved). Public `@a16njs/models@0.12.0` surface unchanged (no semver break). Ready for `/niko-qa`.
- 2026-04-21: Post-preflight operator review surfaced two plan corrections. (1) **Semver bug**: original step 3 proposed changing the semantics of `@a16njs/models::getUniqueFilename`, a re-exported public helper at `0.12.0`. "Strictly safer behavior" is still observable change = breaking change in minor bump. Amended step 3 to plugin-local-only: `plugin-cursor`'s private `getUniqueFilename` (line 66) gets the case-insensitive body internally; `plugin-claude` adds a plugin-local `getUniqueFilenameCI` helper, switches its 5 call sites (187, 360, 437, 498, 618) to it, and drops the `getUniqueFilename` import from `@a16njs/models`. Public `@a16njs/models@0.12.0` surface is byte-identical; third-party `a16n-plugin-*` packages see zero behavior change. (2) **B8 recharacterized**: Cursor implements AgentSkills.io per [cursor.com/docs/skills](https://cursor.com/docs/skills); the spec's `name` field (`[a-z0-9-]+` + "must match parent directory name") binds Cursor's skill dirs identically to Claude's. The current `sanitizePromptName` lowercasing for Cursor skill dirs is **spec-mandated, not a16n convention**. No code change; "deferred follow-up" framing was wrong. Operating principle tightened in `activeContext.md`: preserve case by default across the whole system; the only exceptions are target-spec-mandated. Case handling is per-plugin business, not a shared-model concern. Preflight status re-stamped from PASS_WITH_ADVISORY to PASS (advisory 3 "consolidate uniqueness helpers" dropped entirely as whitebox luxury under the tightened principle). Ready for `/niko-build`.
