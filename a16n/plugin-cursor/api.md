# Cursor Plugin API Reference

> <VersionPicker pkg="plugin-cursor" />

import VersionPicker from '@site/src/components/VersionPicker';

# Cursor Plugin API Reference

Complete API documentation for the `@a16njs/plugin-cursor` package - plugin for reading and writing Cursor IDE configuration files (`.cursor/rules/*.mdc`, `.cursorignore`).

Select a version above to browse the API documentation, or use the sidebar to navigate.
