status: PASS
task_id: 20260421-preserve-filename-case
date: 2026-04-21
amendments_applied:
  - 2026-04-21 (post-preflight operator review):
      - Step 3 rewritten. Public `@a16njs/models::getUniqueFilename` is NOT touched
        (semver preserved; byte-identical behavior at `0.12.0`). Case-insensitive
        collision detection is plugin-local: `plugin-cursor`'s existing private
        `getUniqueFilename` helper (line 66) changes its body; `plugin-claude` adds
        a plugin-local `getUniqueFilenameCI` helper and switches its 5 call sites
        (lines 187, 360, 437, 498, 618) to it, dropping the `getUniqueFilename`
        import from `@a16njs/models`.
      - Rationale per operator: case policy is a plugin-internal concern. A hypothetical
        future third-party plugin (e.g., `a16n-plugin-AgenticHarnessX`) may have
        different rules and implements them itself. Pushing case handling into the
        shared helper would be whitebox coupling that pre-decides policy for all
        consumers.
      - B8 (Cursor skill-dir lowercasing) recharacterized: Cursor implements AgentSkills.io
        per https://cursor.com/docs/skills; the spec's `name` field (`[a-z0-9-]+` + "must
        match parent directory name") binds Cursor's skill dirs identically to Claude's.
        Current lowercasing is spec-mandated, not a16n convention. No code change; B8
        is "no change needed" (was framed as "deferred follow-up" — that framing was wrong).
      - Description, Design Decision table row 8, and Challenges section updated
        accordingly in `tasks.md`.
convention_compliance:
  - PASS. Proposed names `sanitizeRuleFilename`, `sanitizeRuleStem`, `sanitizeSkillDirName`
    follow the plugin-level function-naming convention (`sanitize<Intent>`) already
    present in both plugins (`sanitizePromptName`).
  - PASS. Call-site split cleanly separates rule-filename sanitation (case-preserving)
    from skill-dir-name sanitation (spec-mandated lowercase), matching the intent
    split documented in `memory-bank/systemPatterns.md` §Skill Directory Model and
    §Discovery/Emit Asymmetries.
  - PASS. Regex-widening approach (`[^A-Za-z0-9]+`) aligns with the existing
    sanitization pattern; only the case-class and the `.toLowerCase()` call change.
dependency_impact:
  - `packages/models/src/helpers.ts::getUniqueFilename` is public API (re-exported from
    `packages/models/src/index.ts:39`, published as `@a16njs/models@0.12.0`). Amended
    plan leaves it UNTOUCHED. Existing unit tests in `packages/models/test/helpers.test.ts`
    stay green unchanged. Zero semver impact. Any third-party `a16n-plugin-*` package
    importing this helper sees no behavior change.
  - `packages/plugin-cursor/src/emit.ts` local helpers (`getUniqueFilename` at line 66,
    `getUniqueCommandFilename` at line 149) are plugin-internal (not exported). The
    first gets a case-insensitive behavior change in its private body; the second is
    untouched (ManualPrompts still pre-lowercase via `sanitizePromptName`).
  - `packages/plugin-claude/src/emit.ts` drops its `getUniqueFilename` import from
    `@a16njs/models` and replaces it with a plugin-local `getUniqueFilenameCI`.
    Zero external observable effect.
  - Docs: no rule-filename casing references found in
    `packages/docs/docs/plugin-{claude,cursor}/index.md` or cli docs. The only
    "kebab-case" mention is about the IR `type` enum (plugin-a16n). Step 5 of
    the plan will likely find nothing to change; `projectbrief.md` step 5
    already acknowledges this.
conflict_detection:
  - PASS. `packages/engine/src/path-rewriter.ts::buildMapping` uses the
    source→target mapping table from `WrittenFile.sourcePaths` /
    `sourceItems[*].sourcePath`, not naming conventions — preserving source
    case on the target does not affect reference rewriting.
  - PASS. No existing implementation overlaps the proposed
    `sanitizeRuleFilename` / `sanitizeSkillDirName` split. Current
    `sanitizeFilename` conflates both intents; the split removes duplication,
    not creates it.
  - PASS. No public contract changes at all (down from "one public API" in the
    original plan). `@a16njs/models` public surface is byte-identical after the fix.
  - PASS. No fixture has a mixed-case stem (surveyed every `.md`/`.mdc`/
    `SKILL.md` basename under `packages/plugin-claude/test/fixtures/**`,
    `packages/plugin-cursor/test/fixtures/**`,
    `packages/cli/test/integration/fixtures/**` — all lowercase-kebab).
    B10 ("existing lowercase sources regress to same output") is satisfied
    without touching any fixture.
completeness_precheck:
  - B1 (Claude FileRule preserves case): step 1 → `sanitizeRuleFilename` at
    plugin-claude/src/emit.ts:435. Concrete.
  - B2 (Claude GlobalPrompt preserves case): step 1 → `sanitizeRuleStem`
    (rename of `sanitizeName`) at plugin-claude/src/emit.ts:358. Concrete.
  - B3 (Claude AgentSkillIO skill dir lowercase): step 1 →
    `sanitizeSkillDirName` at plugin-claude/src/emit.ts:186. Concrete.
  - B4 (Claude SimpleAgentSkill skill dir lowercase): step 1 →
    `sanitizeSkillDirName` at plugin-claude/src/emit.ts:497 (fallback branch).
    The `sanitizePromptName` branch (line 496) already lowercases — unchanged.
    Concrete.
  - B5 (Claude ManualPrompt skill dir lowercase): step 1 → `sanitizePromptName`
    at plugin-claude/src/emit.ts:616 unchanged. Concrete.
  - B6 (Cursor FileRule preserves case): step 2 → `sanitizeFilename` body
    change at plugin-cursor/src/emit.ts:532 (via line 31 helper). Concrete.
  - B7 (Cursor GlobalPrompt preserves case): step 2 → inline stem edit at
    plugin-cursor/src/emit.ts:471. Concrete.
  - B8 (Cursor skill dir case — NO CHANGE, spec-compliant): Cursor implements
    AgentSkills.io; lowercasing is spec-mandated via §name + parent-directory
    rule. Behavior is correct; no code change.
  - B9 (case-insensitive collision detection): step 3 (amended) → plugin-local
    only, in both plugins. Public helper untouched.
  - B10 (existing lowercase sources regress to same output): step 4 → no code
    change; full suite stays green. Concrete.
  - B11 (leading-dot filenames still sanitize as before): covered by step 1
    regex widening (dots are not alphanumeric under either the old or new
    regex, so they are still stripped). Test added in step 1.
  - Test coverage planned for every new behavior (B1–B11) across
    `packages/plugin-claude/test/emit.test.ts`,
    `packages/plugin-cursor/test/emit.test.ts`,
    with B10 verified by existing suite. No new test files required.
advisory_findings:
  - Advisory 1 (RETAINED): Consider adding a quick unit test for the new
    plugin-local case-insensitive collision behavior in each plugin's
    `emit.test.ts` (this is where B9 lives anyway, so it's natural — just a
    framing note that the test is now exclusively plugin-scoped, not
    models-scoped).
  - Advisory 2 (RETAINED): For symmetry with the Claude plugin rename
    (`sanitizeFilename` → `sanitizeRuleFilename` + `sanitizeSkillDirName`),
    consider a parallel rename in plugin-cursor's `sanitizeFilename` →
    `sanitizeRuleFilename` since all cursor call sites (line 259, 532, 593)
    are semantically rule-filename sanitization. Cosmetic; zero behavior
    change. Defer if diff noise is a concern.
  - Advisory 3 (REMOVED): "Consolidate the three filename-uniqueness helpers
    behind the public helper." Dropped per operator principle — case policy
    is plugin-internal business, not a shared-model concern. Consolidation
    would be whitebox luxury presuming all consumers want the same policy.
next_step: Proceed to /niko-build.
