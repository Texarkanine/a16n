status: PASS
task_id: 20260421-preserve-filename-case
date: 2026-04-21
phase: QA
summary: >
  Semantic review of the build output against the original plan. All 6 plan
  steps implemented in full with test coverage. Operator-confirmed
  real-world repro against /tmp/niko-test shows rule filenames now preserve
  source case (activeContext.md, productContext.md, systemPatterns.md,
  techContext.md, etc. all emit correctly) while skill directories correctly
  stay lowercase per AgentSkills.io spec. No substantive issues found; no
  trivial fixes needed.
findings:
  - code: KISS
    finding: >
      Plugin-claude's 3-function split (sanitizeRuleFilename /
      sanitizeRuleStem / sanitizeSkillDirName) with shared
      normalizeStemPreservingCase helper is semantically load-bearing — it
      encodes the "case handling by intent, not by call site" design
      decision from the plan. Plugin-cursor stayed minimal (in-place regex
      widening), matching the plan's asymmetric approach. Naming asymmetry
      between plugins is deliberate and disclosed in activeContext.md
      §Deviations.
    severity: none
    action: none
  - code: DRY
    finding: >
      One minor duplication: plugin-cursor/src/emit.ts:484 inline regex
      duplicates the stem-normalization logic from sanitizeFilename body.
      Documented with a comment explaining why (sanitizeFilename would
      double-strip basename/ext for a pre-derived stem). Extracting a
      helper for one call site would be worse than the current shape.
    severity: none
    action: none
  - code: YAGNI
    finding: >
      No speculative code. Every change in emit.ts (both plugins) maps
      directly to a B1–B11 plan behavior. No unused parameters, no
      just-in-case branches.
    severity: none
    action: none
  - code: Completeness
    finding: >
      All 11 plan behaviors (B1–B11) have tests and all pass. Plan steps
      1–6 all executed. Step 4 (fixture sweep, B10) validated by existing
      suite staying green. Step 5 (docs) validated — grep across
      packages/docs found no mention of rule-filename lowercasing or
      kebab-casing that needed correction; the only hit was the `type`
      frontmatter field in plugin-a16n/index.md, which is unrelated.
    severity: none
    action: none
  - code: Regression
    finding: >
      All pre-existing tests pass unchanged except the 2 cursor
      "filename sanitization" tests (lines 137, 216 in test/emit.test.ts)
      that asserted the old lowercasing — updated to assert the corrected
      case-preserving behavior. This update is disclosed in
      activeContext.md §Deviations and is the expected outcome of B10 in
      reverse (fixtures with mixed-case sources now produce mixed-case
      outputs). Both plugins' call-site style and error handling remain
      consistent with surrounding code. No architectural patterns broken.
    severity: none
    action: none
  - code: Integrity
    finding: >
      No debug artifacts, TODOs, magic numbers, or placeholder strings
      introduced. Minor note: sanitizeRuleStem falls back to
      'global-prompt' while the plan said 'rule'. This is a semantic
      improvement (sanitizeRuleStem is only used for GlobalPrompt.name, so
      a more informative fallback is appropriate) and is only reached on
      pathological all-non-alphanumeric inputs. Not a deviation worth
      flagging to the operator.
    severity: none
    action: none
  - code: Documentation
    finding: >
      Memory bank (tasks.md, progress.md, activeContext.md) updated to
      reflect BUILD phase completion and the case-preservation rationale.
      No user-facing docs required updating (confirmed by grep across
      packages/docs/docs for lowercase|kebab|normalized.filename —
      only hit was unrelated `type` frontmatter field description).
      systemPatterns.md §WrittenFile.sourcePaths from the prior task
      remains accurate; no new patterns introduced here.
    severity: none
    action: none
validation:
  node_version: v22.15.0
  typecheck: 12/12 pass
  tests: 15/15 turbo tasks pass
  test_counts:
    cli: 175/175 pass
    engine: 173/173 pass
    plugin-claude: 144/144 pass
    plugin-cursor: 137/137 pass
    glob-hook: 19/19 pass
    models: 33/33 pass
    plugin-a16n: 66/66 pass
  lint: no lint scripts defined in in-scope packages (pre-existing, out of scope)
  operator_verification: >
    Operator confirmed /tmp/niko-test tree now shows all CamelCase rule
    filenames (activeContext.md, productContext.md, systemPatterns.md,
    techContext.md, memory-bank-paths.md, etc.) preserved correctly in
    .claude/rules/**, and skill directories stay lowercase per
    AgentSkills.io spec.
environment_note: >
  Investigated a transient discrepancy where the Cursor agent's Shell tool
  initially reported Node v20.18.2 despite nvm default being v22.15.0.
  Root cause: /home/mobaxterm/.cursor-server/bin/<hash>/node is earlier on
  PATH than the nvm-managed node, shadowing it inside the agent shell
  specifically (user's normal terminals and fresh terminal tabs don't
  have the cursor-server bin dir on PATH). Workaround for QA run: prepend
  /home/mobaxterm/.nvm/versions/node/v22.15.0/bin to PATH. All validation
  on this record was performed under Node 22 after that adjustment.
next_phase: Reflect
