status: PASS_WITH_ADVISORY
task_id: 20260420-skills-docs-and-rewrite-resources
date: 2026-04-20
amendments_applied:
  - tasks.md Behavior 1: replaced 2-condition skip summary with full classification priority
    for both Cursor and Claude, derived directly from discover.ts source.
  - tasks.md Step 6 (plugin-cursor docs): now enumerates complex + simple skill outcomes
    matching the discover.ts priority order; calls out that complex-skill skip is on
    missing `description` regardless of `disable-model-invocation`.
  - tasks.md Step 7 (plugin-claude docs): now enumerates the hooks-first priority and
    all three skip conditions (hooks, complex-missing-description, simple-no-activation).
  - tasks.md Step 8: added `pnpm --filter @a16njs/docs build` to validation since docs
    changes are central to this task and Docusaurus is excluded from default `pnpm build`.
advisory_findings:
  - Consider adding a claude→cursor AgentSkillIO integration test (mirror of the planned
    cursor→claude CI test) in step 5 to cover the Behavior 4 (claude emit) code path
    end-to-end. Symmetric, small extension to existing work.
  - systemPatterns.md documents the Claude SKILL.md classification priority but not the
    Cursor one. A follow-up (out of scope) could add the Cursor priority order for parity.
  - [PROMOTED] Broader refactor: every plugin could populate `WrittenFile.sourcePaths`
    explicitly, allowing `buildMapping` to drop its `sourceItems`-based fallback. Captured
    as a Level 3 follow-up in `planning/writtenfile-clean-break.md`. Do NOT attempt in
    this task. The Behavior 2b collision-detection lint (added 2026-04-20 amendment)
    gives us runtime safety in the interim.
post_preflight_scope_expansion:
  date: 2026-04-20
  trigger: user request (https://agentskills.io/specification#optional-directories)
  amendment:
    - Flipped "Rewriting references inside the ride-along files themselves" from
      non-goal to bounded in-scope goal. Bounded to the two AgentSkills.io-spec-designated
      text subtrees: `scripts/` and `references/`. `assets/` and any other subtree
      remain untouched (spec permits arbitrary non-text content).
    - Added Behavior 7 (`rewriteContent` files-map rewrite w/ subtree allowlist) and
      Behavior 8 (`detectOrphans` files-map scan w/ same allowlist) to the test plan.
    - Added Step 2b (engine: rewriteContent + detectOrphans extension w/ local
      `isRewritableSkillResource` helper). Helper kept private to
      `packages/engine/src/path-rewriter.ts` to minimize blast radius; promote to
      `@a16njs/models` only if a second caller appears.
    - Expanded Behavior 5 / Step 5 integration test to cover positive cases
      (scripts/, references/) and negative cases (assets/, data/ — must not be rewritten).
    - Added Step 8 (docs): document the subtree policy in
      `packages/docs/docs/cli/index.md` §Path Reference Rewriting with a "What gets
      rewritten" table; mirror a shorter note in understanding-conversions.
    - Complexity stays Level 2: ~30 lines of engine code + tests + docs, no new design surface.
    - `projectbrief.md` non-goals list updated accordingly.
