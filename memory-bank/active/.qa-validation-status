PASS

Task: issue-142-spec-compliance-gates
Level: 3
Date: 2026-07-25

KISS: PASS (one observation, non-blocking - see below)
DRY: PASS (NON_SPEC_FEATURES is the single source of truth; the README table
     and the unit-test coverage assertion both derive from it)
YAGNI: PASS with one observation
Completeness: PASS (all 11 steps and all 7 acceptance criteria discharged;
     no TODOs, stubs, or placeholders introduced)
Regression: PASS (no orphaned references to the deleted gate anywhere in
     source, tests, fixtures, or docs; new code matches surrounding style)
Integrity: PASS (no debug artifacts, magic numbers, or hardcoded shortcuts)
Documentation: PASS (5 docs surfaces + systemPatterns.md updated in-commit)

Mechanical verification (re-run at QA time, not trusted from build):
  pnpm build     PASS
  pnpm typecheck PASS
  pnpm test      PASS - 1042 tests, 9 packages, zero failures
  pnpm lint      vacuous - executes zero tasks (no package defines `lint`)

Fixes applied during QA:
  1. spec-compliance.ts: positional-argument pattern /\$[0-9]/ -> /\$[1-9]/.
     $0 is not a Claude positional (the label reads "$1 positional arguments"
     and the deleted gate used [1-9]); the broader class also matched prices
     like "$100" once the argument gate was open.
  2. spec-compliance.ts: removed the `named-arguments` ($name) feature and
     declaredArgumentNames(). Proven empirically against the built detector
     that it never fires without `arguments:` also firing, on any YAML shape.
     Raised as an observation, then resolved by operator principle: our job
     is not to lint people's files, but to convert them faithfully and be
     honest about when we cannot. Enumerating $name usage is cataloguing the
     author's content; reporting that `arguments:` does not survive is the
     honest statement. Also removed its test case and README cell, and
     recorded the principle in systemPatterns.md.
     Warning counts unchanged on every input. Suite 1042 -> 1041, by exactly
     the deleted case.

  3. spec-compliance.ts: removed `positional-arguments` ($1) on the same
     principle, generalized by the operator: detectors that cannot fire alone
     get cut; keep only the top-level detectors needed so that a warning IS
     emitted when something that should warn happens. Its gate was literally
     another feature's presence, so $ARGUMENTS / arguments: / argument-hint:
     always reported first. I argued to keep the argument-hint-gated path
     (companion is a mere display hint, so it understates body breakage);
     overruled in favour of the general rule. Removed the feature, its gate,
     declaresArguments, two $N-gating tests and one tautological negative;
     updated the ordering test, README, and docs-site list.
  4. spec-compliance.test.ts: per-feature assertion tightened from
     toContain(label) to toEqual([label]). This converts the fire-alone rule
     from convention into a CI failure for any future detector that cannot
     warn on its own. Recorded in systemPatterns.md and the module header.

Net effect of fixes 2-4: NON_SPEC_FEATURES 15 -> 13, suite 1042 -> 1038.
Warning COUNTS unchanged on every possible input; only label lists shorten.

Open, routed out of QA:
  1. techContext.md's "Full validation" line lists `pnpm lint`, which runs
     zero tasks. Pre-existing and out of this task's scope.
