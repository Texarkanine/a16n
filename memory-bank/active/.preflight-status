PREFLIGHT: PASS (with advisory)
task: v1-release-rollout-m2
date: 2026-06-13
phase: PREFLIGHT
complexity: Level 3
summary: M2 release-pipeline-hardening plan validated against codebase reality. TDD ordering is explicit per testable unit (manifest-access guard, pure analyzer); IO/glue steps (entry script, release.yaml wiring) sit over the tested analyzer. All requirements (R1-R6) map to concrete steps with tests. No blocking findings.
advisories_resolved_by_operator:
  - Guard language: keep plain ESM .mjs; add a concise rationale header in the guard file (step 5).
  - Test overlap: remove packages/plugin-agentsmd/test/publish-shape.test.ts (step 3), sequenced after the repo-wide access guard so coverage is preserved.
  - Radical/PR-time guard: brought IN SCOPE — reuse analyzer in ci.yaml PR-mode (step 7). Brief + ACs updated.
amendment_note: Plan amended post-preflight; additions are additive and reuse the validated analyzer (no new design ambiguity). Preflight PASS still holds.
gate: cleared for /niko-build
