---
task_id: issue-161
complexity_level: 2
date: 2026-08-20
status: completed
---

# TASK ARCHIVE: Clear leftover oxlint unused-vars in glob-hook and docs

## SUMMARY

Cleared the two leftover Oxlint `unused-vars` findings from [#161](https://github.com/Texarkanine/a16n/issues/161): unused `HookInput` type import in `packages/glob-hook/test/io.test.ts`, unused `dirname` import in `packages/docs/scripts/generate-cli-docs.ts`. Grouped oxlint is clean; both package test suites pass. No extra rule categories, no CI, no change-detector tests.

## REQUIREMENTS

1. Clear both grouped leftovers in one change set.
2. `pnpm exec oxlint packages/glob-hook packages/docs` clean.
3. `pnpm --filter @a16njs/glob-hook test` and `pnpm --filter docs test` still pass.
4. Do not enable extra Oxlint categories, add lint to CI, or write change-detector tests.

## IMPLEMENTATION

Two unused-import deletions. `HookOutput` stays (used by `writeOutput` tests). `join` stays (used throughout the CLI docs generator). `oxlint --fix` does not remove these; the edits are manual.

## TESTING

Oxlint was already the red check. No new Vitest cases (a test that only fails if the unused import returns is a change-detector). `@a16njs/glob-hook` 37/37; `docs` 56/56; grouped oxlint clean. `/niko-preflight` PASS; `/niko-qa` PASS.

## LESSONS LEARNED

- `oxlint --fix` still does not remove unused imports; leftover unused-vars need a manual delete.
- Unused-import deletion is not new executable behavior. Oxlint is the checker (same as #74).
- The elegant form is never importing an unused name. These leftovers are inventory from the #74 bind, not a missing abstraction.

## PROCESS IMPROVEMENTS

Grouping two packages under one leftover ticket routes the complexity tree to Level 2 even when each edit is one token. That matches "stay grouped" and is more process than design. Single-package leftovers (e.g. #156) stay Level 1.

## TECHNICAL IMPROVEMENTS

None. Extra Oxlint categories remain a later choice, not this leftover ticket.

## NEXT STEPS

None for this ticket. Remaining leftover oxlint issues are other packages.
